THE LATEST PROJECT WORK SUMMARY
Updated: 2026-08-26

PROJECT OVERVIEW
The Latest has been built into a multi-surface news aggregation product focused on live news, opinions, podcasts, videos, social signals, source trust context, and side-by-side perspective comparison. The project has evolved from a basic RSS-driven frontend into a broader editorial product with source governance, server-side enrichment, search UX improvements, analytics, and deployment-readiness tooling.

PRIMARY PRODUCT DIRECTION
- RSS-first aggregation with optional API-backed expansion.
- Stronger source transparency and trust context to differentiate from competitors like Ground News.
- Lower-cost architecture where provider APIs are fallback or supplemental layers, not the default for every request.
- Improved editorial polish on homepage cards, search landing, and comparison surfaces.
- Better measurement of user engagement across trust and comparison features.

MAJOR COMPLETED WORK

1. HOMEPAGE AND CARD DESIGN
- Refined the Top Stories side-by-side cards to look smaller, tighter, and more editorial.
- Eliminated excess whitespace and footer voids in comparison cards.
- Matched image proportions more cleanly with card content.
- Added a second stacked comparison card below the main side-by-side lead card.
- Improved content sizing and card density for a more professional presentation.

2. DATE NORMALIZATION ACROSS SURFACES
- Replaced vague "Latest" labels with actual publish dates wherever possible.
- Centralized date handling in shared utilities.
- Applied publish-date normalization across top stories, opinions, podcasts, and social surfaces.
- Propagated normalized timestamps into full listing pages as well.

3. SOURCE TRUST AND PROFILE LAYER
- Added explicit source-profile links across homepage cards and comparison surfaces.
- Built a static source profile registry for major outlets.
- Added richer trust descriptors and source-level context.
- Enabled explicit trust score overrides from a managed backend registry.
- Integrated backend-managed source metadata into the source page and article reader.
- Extended trust and perspective handling into search and comparison surfaces.
- Fixed null trust-score coercion in both registry merge and render-layer trust resolution so live truth badges do not collapse to zero when managed scores are blank.
- Expanded canonical source-family coverage so feed variants and syndicated inputs resolve back to curated outlet profiles more reliably.

4. SOURCE REGISTRY AND BACKEND GOVERNANCE FOUNDATIONS
- Added a public GET endpoint for managed source records.
- Added admin-gated POST support for ownership, perspective, trust score, notes, and active status.
- Added service-side registry merging so backend overrides can layer over static source profiles.
- Added source-governance audit tooling to quantify missing metadata.
- Seeded managed-source governance from the curated frontend registry for known major outlets.
- Reworked the governance audit to distinguish baseline resolved coverage from deeper outlet curation coverage.
- Curated multiple follow-on batches across news, business, tech, sports, culture, entertainment, opinion, lifestyle, and public-media podcast brands.

5. STORY CLUSTERING AND PERSPECTIVE COMPARISON
- Added more stable cluster identity generation with hashed cluster IDs and keys.
- Improved same-story grouping across outlets.
- Added source-based perspective fallback when explicit classification is unavailable.
- Carried comparison metadata into side-by-side surfaces.
- Added comparison analytics for views, story clicks, source selection, and profile clicks.

6. SEARCH EXPERIENCE REWORK
- Reworked the search landing page.
- Added starter search cards for no-query state.
- Added overview cards for active searches.
- Added best-starting-point cards for strong exact/high-signal matches.
- Added registry-backed trust and perspective resolution to result cards.
- Added search view and result-click analytics.
- Preserved AI search-assist behavior while improving the core landing and discovery flow.

7. RSS INGESTION AND PROVIDER EXPANSION
- Added support for dynamic ADDITIONAL_RSS_FEEDS configuration.
- Added support for EXTERNAL_NEWS_PROVIDERS configuration.
- Implemented NewsAPI and GNews provider parsing in the backend aggregator.
- Allowed provider-backed feeds to supplement category coverage.
- Added provider-backed live backfill to opinions, videos, and podcasts before generated fallback.
- Expanded provider groups for politics, tech, business, entertainment, sports, culture, and lifestyle.

8. SOCIAL FEEDS AND FEED CONFIGURATION
- Changed SOCIAL_RSS_FEEDS behavior so env-defined feeds extend the built-in list instead of replacing it.
- Fixed duplicate helper issues in social feed loading.
- Repaired broken social feed routes with direct publisher feeds where necessary.
- Added Reddit and Hacker News supplemental social/topic feeds.

9. FEED AUDIT, PRIORITIZATION, AND QUALITY CONTROL
- Audited live feeds for dead or failing upstream sources.
- Removed or disabled broken built-in video and podcast feeds that were wasting fetch slots.
- Promoted high-value opinions and video feeds to higher priority.
- Preserved RSS-first operation while improving actual live coverage quality.

10. ANALYTICS AND ENGAGEMENT TRACKING
- Added dossier section engagement analytics earlier in the project.
- Added search view and result-click analytics.
- Added comparison analytics on the side-by-side surface.
- Kept analytics consent-aware via the existing consent flow.

11. SEARCH, DOSSIER, AND AI FALLBACK BEHAVIOR
- Preserved server-backed editorial summary behavior.
- Added better fallback routing between strict and relaxed search modes.
- Kept generated-content fallback available when live RSS or provider coverage is thin.
- Added env verification around Anthropic and fallback model configuration.

12. DEPLOYMENT AND DEV-TOOLING HARDENING
- Added deploy environment verification scripts.
- Added Blob credential checks for non-Netlify runtimes.
- Added deployed smoke-test tooling.
- Added source-governance audit tooling.
- Fixed the local dev-netlify launcher so it no longer falsely selects a conflicting port.
- Added regression tests for provider backfill logic and dev-netlify port resolution.
- Fixed Node-compatible source profile import behavior for backend seeding.
- Marked netlify/functions/package.json as type commonjs to prevent Netlify from loading CommonJS functions as ESM.

13. LIVE PRODUCTION DEBUGGING, SUMMARY RECOVERY, AND RSS PACKAGING FIXES
- Investigated a live-site regression where shared devices and first-load users were seeing generated fallback content instead of live RSS coverage.
- Confirmed the server-side shared summary endpoint could still return Claude-backed summaries while the homepage UI was restoring stale local fallback state.
- Updated homepage summary caching so model-free local editorial summaries no longer count as strong cache entries.
- Bumped the homepage summary cache version to invalidate stale localStorage entries that survived hard refreshes.
- Traced a separate production outage to the public rss-aggregator function entrypoint being loaded with the wrong module mode in Netlify.
- Split the RSS aggregator into a stable CommonJS implementation file and a public wrapper entrypoint so Netlify can load the public function while internal callers continue using CommonJS.
- Repeated live deploy validation against the production URL and unique deploy URLs until the public RSS endpoint returned real live feed items again.
- Confirmed live shared summaries still return Claude-backed topics after the RSS packaging fix.

14. EXECUTIVE / REPORTING SUPPORT
- Produced prior executive-facing summary work for leadership communication.
- Consolidated product viability direction around trust, comparison, and coverage differentiation.

CURRENT REPO STATUS
- Build passes locally.
- Focused Vitest coverage passes for source profiles, source registry service, provider fallback logic, and dev-netlify port resolution.
- Deploy environment verification now passes locally.
- Managed source governance seeding works in the local backend module for known curated outlets.
- Source profile regression coverage now includes canonical outlet mapping, variant-family mapping, second-batch curation coverage, and opinion/podcast family resolution.
- Current curated source coverage is 159 of 189 managed source records, leaving 30 uncurated records across 29 unique outlets.
- The RSS aggregator now uses a split-entry packaging approach:
  - netlify/functions/rss-aggregator-impl.cjs holds the CommonJS implementation.
  - netlify/functions/rss-aggregator.cjs is the internal CommonJS bridge for other functions.
  - netlify/functions/rss-aggregator.js is the public wrapper entrypoint for Netlify.

CURRENT KNOWN PRODUCTION STATUS
- Production URL checked: https://resonant-bombolone-87d70d.netlify.app
- Live deploys were iterated until the public RSS endpoint stopped failing with Netlify module-loading/runtime packaging errors.
- Current live deploy verified: 6a7f39c99cc10571cd92788d.
- The public RSS endpoint now returns HTTP 200 and real aggregated feed data again.
- The shared summary endpoint still returns Claude (claude-sonnet-5) topics on live production.
- Raw homepage HTML no longer has The Latest Editorial fallback baked into the delivered shell.
- Truth-score rendering was also fixed so blank managed trust values no longer render as zero in production.
- The main remaining production follow-up is browser-level smoke verification of the hydrated homepage and major browse surfaces after the live RSS restoration.

DEPLOYED SOURCE-GOVERNANCE STATUS
- The governance model is now much stronger locally than this file previously reflected.
- Current validated local/source-resolution state shows:
  - Total managed sources: 189
  - Resolved governance coverage: 189 of 189
  - Curated coverage: 159 of 189
  - Remaining uncurated records: 30
  - Remaining unique outlets: 29
- Recently added curated coverage includes examples such as:
  - Channel NewsAsia
  - Fortune
  - Billboard
  - Apartment Therapy
  - South China Morning Post
  - National Review
  - Marketplace
  - The Hollywood Reporter
- Remaining backlog is now concentrated in long-tail culture, lifestyle, podcast, and secondary trade outlets rather than major unclassified newsrooms.

CURRENT ENV / CONFIG STATUS
- SESSION_TOKEN_PEPPER is now set and verify:deploy-env passes.
- ADMIN_EMAILS is now set (24.chernandez@gmail.com) locally and across all three hosted Netlify contexts (production, deploy-preview, branch-deploy).
- Remaining verifier warnings are reduced to:
  - RSS_APP_AUTH_TOKEN not set
- Notes instead of hard warnings now cover:
  - Browser AI intentionally disabled via VITE_AI_PROVIDER=editorial
  - RSS bundle feed URL/source using built-in defaults
  - ANTHROPIC_CONTENT_FALLBACK_MODEL falling back to summary model/default

WHAT IS STILL INCOMPLETE
1. Browser-level production smoke verification still needs to be rerun against the current live site.
  Reason: endpoint-level validation is current, but a full hydrated-browser pass across homepage and browse surfaces has not yet been rerun after the final RSS packaging deploy.
   Commands:
   - npm run smoke:netlify-preview -- https://your-site.netlify.app
   - npm run audit:sources -- https://your-site.netlify.app

2. The final source-curation backlog is not finished yet.
   Reason: 30 managed source records across 29 unique outlets still resolve to fallback "Needs review" metadata.

3. The remaining backlog is now mostly long-tail outlets rather than core governance gaps.
   Reason: the unresolved set is now concentrated in lifestyle, culture, podcasts, and secondary trade brands rather than the core newsroom set.

4. RESOLVED: ADMIN_EMAILS is now set to 24.chernandez@gmail.com (a personal address, used as a placeholder until an Our National Conversation org/domain email is provisioned) both locally and in all three hosted Netlify contexts.

5. RSS_APP_AUTH_TOKEN is only needed if the RSS.app bundle feed is private.
   If the bundle feed is public and your current API key/secret path is sufficient, this may not be a blocker.

6. RESEND_API_KEY is not set yet, so password-reset emails are only logged to the Netlify function console, not actually delivered to users.
   Reason: no transactional email provider is configured. The reset flow itself is fully built and tested; this is the one remaining step before real users can actually receive reset emails.
   Action: create a Resend account, verify a sending domain, and set RESEND_API_KEY + EMAIL_FROM in Netlify (see SECRETS_SETUP.md).

RECOMMENDED NEXT STEPS
1. Run deployed smoke testing against the real production URL now that the RSS endpoint is returning live data again.
2. Run the deployed source-governance audit again so the live numbers can be compared against the current local 159-of-189 curated state.
3. Curate the remaining long-tail outlet backlog, starting with lifestyle, culture, podcasts, and secondary trade brands.
4. DONE: ADMIN_EMAILS is set. Swap it to an org/domain email once Our National Conversation provisions one, instead of relying on a personal Gmail long-term.
5. Only set RSS_APP_AUTH_TOKEN if the RSS.app bundle feed is private.
6. Set up a Resend account and RESEND_API_KEY/EMAIL_FROM before launch so password-reset emails actually send instead of only being logged.

KEY FILES TO KNOW
- src/pages/SearchResults.jsx
- src/pages/SearchResults.css
- src/components/sections/TopStories.jsx
- src/utils/dateUtils.js
- src/utils/sourceProfiles.js
- src/services/sourceRegistryService.js
- src/newsService.js
- src/components/sections/AISummary.jsx
- netlify/functions/rss-aggregator.cjs
- netlify/functions/rss-aggregator.js
- netlify/functions/rss-aggregator-impl.cjs
- netlify/functions/sources.js
- netlify/functions/clusters.js
- netlify/functions/fetchSocialFeeds.js
- netlify/functions/trackEngagement.js
- scripts/verify-deploy-env.mjs
- scripts/smoke-netlify-preview.mjs
- scripts/audit-source-governance.mjs
- scripts/dev-netlify.mjs
- netlify/functions/package.json

VALIDATION COMPLETED LOCALLY
- npm run build
- npm run verify:deploy-env
- npm exec vitest run src/utils/sourceProfiles.test.js src/services/sourceRegistryService.test.js src/newsService.test.js scripts/dev-netlify.test.js
- npm exec vitest run src/utils/sourceProfiles.test.js
- direct Node inspection of netlify/functions/rss-aggregator.cjs for managed source seeding

FINAL HANDOFF NOTE
The project has moved well beyond a simple RSS reader. It now has a meaningful trust layer, comparison UX, source governance foundations, provider-assisted resilience, analytics, and deploy-readiness tooling. The main short-term blocker is not feature breadth. It is making sure the latest backend packaging fix is deployed and then verifying live production behavior against that updated deployment.

LATEST LIVE VALIDATION NOTE
- Live production RSS endpoint recovered after entrypoint packaging fixes and redeploy.
- Live shared summary endpoint returns Claude-backed output.
- Homepage summary cache handling was tightened so stale model-free editorial fallback does not out-rank stronger shared-summary data.
- Remaining validation should focus on end-to-end browser behavior rather than raw endpoint health.

CLAUDE AUDIT HARDENING CHANGES COMPLETED (08/19/2026)
- Reviewed and implemented the concrete launch-readiness items surfaced by the later Claude audit rather than treating the audit as a blind patch list.
- Confirmed the active production AI summary path is server-side Anthropic via Netlify Functions, not a browser-exposed Claude key path.

1. SECURITY AND ENV HARDENING
- Added baseline security headers to netlify.toml, including Content-Security-Policy with base-uri/frame-ancestors/object-src restrictions, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, and Permissions-Policy.
- Removed the browser-side VITE_ANTHROPIC_API_KEY path from src/services/aiService.js so Anthropic is no longer a frontend provider option in the shipped client code.
- Kept Claude-backed summary and perspective behavior server-side through ANTHROPIC_API_KEY in Netlify Functions.
- Updated env examples, deployment docs, release checklist guidance, and secret-setup tooling so they no longer suggest configuring VITE_ANTHROPIC_API_KEY for hosted environments.
- Strengthened scripts/verify-deploy-env.mjs so VITE_ANTHROPIC_API_KEY is treated as a real misconfiguration rather than a soft warning if it ever shows up again.

2. LIVE NETLIFY CONFIG VERIFICATION
- Queried the linked Netlify site directly across dev, production, deploy-preview, and branch-deploy contexts.
- Verified that VITE_ANTHROPIC_API_KEY is not present in the hosted environment.
- Verified that the live Anthropic-related variables currently present are limited to:
  - ANTHROPIC_API_KEY
  - ANTHROPIC_PERSPECTIVE_MODEL
  - ANTHROPIC_SUMMARY_MODEL
- This confirms the cost-risk called out in the Claude audit was checked directly against the real Netlify project, not only inferred from repo code.

3. CLAUDE-AUDIT PRODUCT / ROUTING CLEANUP
- Added a real NotFoundPage and catch-all route so unknown paths no longer silently fall through the SPA without a proper 404 surface.
- Removed dead fake subscribe plumbing from App.jsx instead of preserving unused alert/console behavior.
- Updated the TopStories regression test to match the current side-by-side UI and wrapped the slice with ConsentProvider.
- Corrected stale release checklist test instructions so the documented validation path matches the current project.

4. DOMAIN / SITE CONFIG CLEANUP
- Centralized the canonical site URL and contact-email configuration through shared site config utilities.
- Replaced hardcoded thelatest.news references across footer, legal/contact pages, and article-fetch User-Agent handling.
- Made the SEO metadata site URL safe for both Vite runtime access and Node-side prerender/build imports.

5. LINT / DEV-ERGONOMICS CLEANUP
- Split ad slot and size presets out of AdBreak.jsx into a dedicated presets module so the prior Fast Refresh warning is gone.
- Cleared the focused TopStories and AdBreak test slices after the audit-driven changes.
- Preserved the earlier lint/parser fix for the rss-aggregator Netlify wrapper while keeping the current hardening pass clean.

LATEST CLAUDE-AUDIT VALIDATION COMPLETED
- npm run lint
- npm test -- AdBreak.test.jsx TopStories.test.jsx
- npm run build
- npm run verify:deploy-env
- netlify env:list
- netlify env:list --context production
- netlify env:list --context deploy-preview
- netlify env:list --context branch-deploy

LATEST CLAUDE-AUDIT OUTCOME
- The browser-exposed Anthropic key concern has been checked both in code and against the live Netlify environment.
- Baseline clickjacking/content-type/referrer hardening is now in place in Netlify headers.
- The repo no longer advertises or depends on a hosted VITE_ANTHROPIC_API_KEY path.
- Remaining operational follow-up is mostly standard production hygiene rather than a blocking Claude-audit security issue.

ADMIN ACCESS, RATE LIMITING, CSP, AND ERROR-REPORTING HARDENING (08/19/2026)
A second Claude-audit follow-up pass, focused on the auth/admin surface, transport-level hardening, and closing the "no production visibility into client errors" gap.

1. ADMIN ACCESS FAIL-CLOSED FIX
- Found that netlify/functions/adminAccess.js granted admin actions to ANY authenticated user whenever ADMIN_EMAILS was unset, instead of denying everyone. This is a real bug, not just a config gap: any deploy that forgot to set ADMIN_EMAILS silently ran wide open.
- Fixed canManageAdminActions() to fail closed: an empty allowlist now denies admin access to everyone instead of granting it to everyone.
- Confirmed admin access here is entirely homegrown (netlify/functions/authStore.js: scrypt password hashing, HMAC-peppered session tokens in Netlify Blobs) -- there is no third-party identity provider or SSO involved. Admin status is purely "is this authenticated account's email in the ADMIN_EMAILS allowlist."
- Set ADMIN_EMAILS=24.chernandez@gmail.com locally (.env) and in Netlify (production, deploy-preview, branch-deploy contexts) via the Netlify CLI, with an inline TODO comment (in .env and .env.example) to swap to an Our National Conversation org/domain email once one exists.
- Updated scripts/verify-deploy-env.mjs's ADMIN_EMAILS guidance text to reflect the new fail-closed behavior.

2. AUTH RATE LIMITING
- netlify/functions/auth.js's register and login actions had no rate limiting at all, despite netlify/functions/rateLimit.js already being used elsewhere (searchAssist, sharedSummary, generatedContent, rss-aggregator).
- Added enforceRateLimit to register (5 requests/min per IP) and login (10 requests/min per IP+email combination), returning 429 with Retry-After when exceeded.

3. CONTENT-SECURITY-POLICY HARDENING
- netlify.toml previously shipped a CSP with only base-uri/frame-ancestors/object-src -- no script-src, style-src, img-src, font-src, or connect-src restriction at all, meaning script and connection sources were effectively unrestricted.
- Added explicit directives: script-src 'self' 'unsafe-inline', style-src 'self' 'unsafe-inline', img-src 'self' data: https:, font-src 'self' data:, connect-src 'self' https://www.reddit.com https://api.openai.com https://api.perplexity.ai, frame-src https://www.youtube.com, media-src 'self' https:.
- script-src and style-src keep 'unsafe-inline' deliberately: SeoManager.jsx injects per-route JSON-LD structured data as a dynamically created inline <script>, and the app renders React inline style={{...}} attributes throughout -- both are static-site constraints (no per-request server templating to hand out a CSP nonce), documented inline in netlify.toml.
- Verified the header live: ran `netlify dev`, confirmed the CSP header is actually served, then drove a headless Playwright browser across home, an article page, /videos (YouTube iframe), a /topic page (direct-from-browser Reddit fetch), and /dashboard. First pass caught one real violation (the JSON-LD inline script); after adding 'unsafe-inline' to script-src, zero CSP violations and zero console errors across all five routes. Confirmed with a homepage screenshot that rendering (including the live server-side Claude AI briefing) was unaffected.

4. LIGHTWEIGHT CLIENT ERROR REPORTING
- There was previously no production visibility into client-side errors -- ErrorBoundary.jsx only did console.error, which is invisible once deployed.
- Added netlify/functions/clientError.js: rate-limited (20/min/IP), truncates and logs incoming error reports via console.error (visible immediately in Netlify function logs) and aggregates them into Netlify Blobs (STORE_NAMES.clientErrors) keyed by day + message/path signature with a running count.
- Added src/services/errorReportingService.js as the client-side POST helper.
- Wired it into ErrorBoundary.jsx's componentDidCatch (React render errors) and into src/main.jsx via window 'error' and 'unhandledrejection' listeners (errors outside the React tree).
- Deliberately did not require signing up for a third-party service (Sentry/Datadog/etc.) -- this gives baseline visibility without a new external dependency or account.

5. NEW SECURITY-FOCUSED TEST COVERAGE
- Added netlify/functions/adminAccess.test.js (10 tests): covers the fail-closed allowlist behavior, case-insensitive matching, and the 401/403/allowed paths of requireAdminAccess.
- Added netlify/functions/rateLimit.test.js (7 tests): window bucketing, per-IP scoping, per-keySuffix (per-account) scoping.
- Added netlify/functions/auth.test.js (3 tests): confirms register/login actually return 429 once their configured thresholds are hit, and that login limits are scoped per email+IP rather than globally.
- Discovered along the way that vi.mock() does not reliably intercept require() calls between sibling CommonJS files in this project's netlify/functions (type: commonjs) directory -- the initial mocked tests were silently exercising the REAL authStore/blobStore code underneath, including live network calls to the real Netlify Blobs store using the real credentials in the local .env file (this both caused test timeouts and briefly wrote a handful of harmless test rate-limit-counter keys into the live Blobs store). Fixed by monkey-patching the sibling module's exports object directly before requiring the module under test, which works correctly under Node's real CJS module-cache semantics. All three new test files now run in-memory only, in well under a second combined.
- Full suite is now 55 tests passing (up from 35), lint clean.

LATEST HARDENING VALIDATION COMPLETED
- npm run lint
- npm test (55 tests passing)
- npm run build
- netlify dev + headless Playwright CSP violation check across 5 routes (0 violations) + homepage screenshot
- netlify env:get / env:set ADMIN_EMAILS across production, deploy-preview, branch-deploy contexts

6. DEPENDENCY SECURITY FIX
- npm audit surfaced 11 high-severity react-router/react-router-dom CVEs (XSS via redirect handling, CSRF, open redirect, DoS via route matching and path expansion) affecting the installed 7.13.0, patched in 7.18.2, plus a form-data CRLF-injection advisory.
- package.json already allowed ^7.13.0, so 7.18.2 was a safe in-range patch bump, not a breaking major-version change. Ran `npm audit fix` (no --force); package.json was untouched, only package-lock.json/installed versions moved. Re-ran the full test suite (55/55 passing) and a production build (243 route shells prerendered) to confirm nothing broke.
- `npm audit` (production deps) now reports 0 vulnerabilities.

LATEST HARDENING OUTCOME
- The admin allowlist can no longer fail open -- an unset or empty ADMIN_EMAILS now denies everyone rather than granting everyone admin actions.
- Auth register/login endpoints are no longer brute-forceable/spammable without any rate limiting.
- The CSP now meaningfully restricts script/style/connect/frame sources instead of having no restriction on any of them.
- Client-side production errors are now visible via Netlify function logs and an aggregated Blobs store, without requiring a new third-party account.
- The two prior fixes (admin fail-closed, auth rate limiting) now have direct regression coverage instead of relying on manual review alone.

HSTS AND ACCOUNT-RECOVERY HARDENING (08/19/2026)
A third same-day follow-up pass covering the two "worth doing before launch" items from the last review round: transport-security headers and a missing password-recovery flow.

1. STRICT-TRANSPORT-SECURITY HEADER
- Added `Strict-Transport-Security = "max-age=31536000; includeSubDomains"` to both header blocks in netlify.toml.
- Deliberately left off the `preload` directive -- submitting to the browser HSTS preload list is a separate, much harder-to-reverse commitment (it affects every subdomain indefinitely and takes a manual removal request to undo) that should be a deliberate later decision, not a side effect of this change.

2. PASSWORD-RESET ("FORGOT PASSWORD") FLOW
- The auth system had register/login/logout but no account-recovery path, meaning a user who forgot their password had no way back into an account with saved follows/dashboard data except abandoning it and re-registering.
- Backend: netlify/functions/authStore.js gained createPasswordResetToken, consumePasswordResetToken (single-use -- the record is deleted on lookup whether or not it's still valid, so a token can never be replayed), and updateUserPassword, all built on the same scrypt/HMAC-pepper primitives already used for sessions. Generalized the session-token-hashing function into a shared digestToken so reset tokens reuse the same SESSION_TOKEN_PEPPER-based hashing rather than introducing a second secret.
- netlify/functions/auth.js gained two new actions: request-password-reset (rate-limited 5/min per email+IP; always returns the same generic "if an account exists..." message and only actually sends an email when the account exists, so the endpoint can't be used to enumerate registered emails) and reset-password (rate-limited 10/min per IP; validates the token, updates the password, and immediately signs the user in with a fresh session so they don't have to log in again right after resetting).
- Added netlify/functions/emailService.js: a minimal Resend-backed email sender (plain fetch call, no new npm dependency). Until RESEND_API_KEY is set, it logs the email (including the reset link) to the function's console instead of throwing, so the whole flow is testable in local dev / netlify dev before any email provider is configured. Documented the RESEND_API_KEY / EMAIL_FROM setup in .env.example, SECRETS_SETUP.md, scripts/set-hosted-secrets.ps1, and scripts/verify-deploy-env.mjs (recommended, not required -- reset emails just get logged instead of sent until it's set).
- The reset link's origin is reconstructed from the incoming request's Host/X-Forwarded-Host/X-Forwarded-Proto headers (falling back to VITE_SITE_URL, then a hardcoded default) rather than depending on an env var that isn't set in every context -- this way the link is correct whether the request came in on a custom domain, the raw *.netlify.app URL, a deploy preview, or local dev.
- Frontend: added a "forgot" mode to the existing LoginModal (a "Forgot password?" link under the password field switches to an email-only form, submits via a new requestReset() on AuthContext, shows a green confirmation message reusing the existing form styling) and a new standalone /reset-password page (src/pages/ResetPasswordPage.jsx, wired into App.jsx) that reads the token from the URL query, collects and confirms a new password, and calls a new completeReset() on AuthContext which both resets the password and persists the returned session (auto-login after reset).
- Added netlify/functions/auth.test.js coverage for both new actions (9 new tests): sends-email-and-generic-message for existing accounts, generic-message-with-no-email for non-existent accounts (enumeration protection), rate limiting on both actions, successful reset + auto-login, and rejection of invalid/expired tokens and too-short passwords.
- End-to-end verified live against `netlify dev`: registered a real test account, requested a reset, confirmed the (unsent, logged) reset link appeared correctly in the function console with the right host and protocol, drove the actual /reset-password page in a headless browser through to its "Password Updated" success screen, then confirmed login with the new password worked. Separately drove the LoginModal's "Forgot password?" click-through end to end (open modal -> Forgot password -> submit email -> green confirmation message), zero console errors. Cleaned up the test account (and its follows record) from the live Netlify Blobs store afterward via `netlify blobs:delete` since this local testing ran against the real hosted Blobs credentials in .env.

3. DEV-ERGONOMICS FIX FOUND ALONG THE WAY
- Running `netlify dev` populates a local `.netlify/functions-serve/` build cache containing copies of everything in netlify/functions/, including the *.test.js files added in the last two rounds. Vitest's default test discovery picked those copies up too, silently doubling the netlify/functions test count (61 real tests briefly reported as 113) any time someone had run `netlify dev` before `npm test`. `.netlify/` was already gitignored so this never affected CI, but it was a real local footgun. Fixed by excluding `.netlify/**` in vite.config.js's test config.

LATEST HSTS/RECOVERY VALIDATION COMPLETED
- npm run lint
- npm test (61 tests passing, confirmed stable after excluding the .netlify/functions-serve cache)
- npm run build (243 route shells prerendered)
- netlify dev + live end-to-end password-reset flow (register -> request reset -> reset via the real page -> login with new password) verified via curl and headless Playwright, with screenshots
- netlify blobs:list / blobs:get / blobs:delete to clean up the test account created during that live verification

LATEST HSTS/RECOVERY OUTCOME
- Repeat visitors' browsers will no longer even attempt a plain-HTTP request to the site for a year after their first HTTPS visit.
- Users who forget their password now have a real, rate-limited, enumeration-safe way back into their account instead of being locked out permanently.
- The reset flow works end-to-end today in every environment (logs the link instead of emailing it until RESEND_API_KEY is set), so there's no hard blocker to shipping it now and wiring up real email delivery whenever a Resend account is set up.
- Confirmed there is no separate database in this project: Netlify Blobs (a key-value blob store) is already the entire user/session/reset-token "database" -- see authStore.js and blobStore.js. This is adequate at the project's current scale (simple get-by-key lookups, no relational queries needed) and required no new infrastructure decision for this feature.

CONTENT-LICENSING / ATTRIBUTION HARDENING (08/19/2026)
Jeff Hall (boss) reviewed the aggregation/attribution posture and gave explicit direction: always credit the original source, show only a small snippet (not the full story), link to the original immediately after the snippet, stand up an unlisted /privatedemo page for backer pitches before going fully public, and add DMCA/takedown language. This pass implements all four asks directly.

1. STOPPED FULL-TEXT ARTICLE SCRAPING -- THE MOST URGENT FIX
- netlify/functions/fetchArticle.js previously scraped and returned the FULL body text of third-party articles (via regex-matched <article>/<main>/articleBody containers), which ArticleReader.jsx then rendered natively on our own /story/... pages with our own ads next to it -- full-text mirroring of other outlets' reporting, not aggregation. This was the single biggest legal exposure identified in this project.
- Added a buildExcerpt() helper (sentence-boundary-aware, ~420 char cap) and changed the response shape from `content` (full text) to `excerpt` + `isExcerpt: true`. We never return more than a couple of sentences of third-party material now, regardless of source article length.
- Restructured ArticleReader.jsx: for real (non-generated) articles, the flow is now short excerpt -> an explicit "this is a short excerpt" disclaimer -> a prominent "Read the full story at [Source] ->" button, immediately after the excerpt. Removed the old "Originally published by" line that was buried at the very bottom of the page below five other sections -- credit now happens where a reader actually sees it. Reading-time estimates ("X min read") are now only shown for our own AI-generated content, since we no longer have a real word count for third-party articles we're intentionally not copying.
- Generated articles (our own original AI briefings) are unaffected -- they still render in full, since that's our own content, not someone else's.
- Verified live: opened a real NPR article through the actual reader in a headless browser; excerpt rendered at 218 characters cut cleanly at a sentence boundary, "Read the full story at NPR ->" button rendered immediately after, zero console errors.

2. DMCA / TAKEDOWN POLICY PAGE
- Added src/pages/DMCAPage.jsx (new /dmca route) with standard notice-and-counter-notice elements consistent with 17 U.S.C. Section 512(c)(3): identification of the work, the infringing URL, contact info, good-faith and perjury statements, signature -- plus a counter-notification section and a repeat-infringer policy statement.
- Linked from the footer alongside the other legal pages, and cross-referenced from the Terms page's Intellectual Property section.
- Added to src/utils/siteRoutes.js (INDEXABLE_STATIC_ROUTES) so it's properly included in the sitemap and gets its own prerendered shell like the other legal pages, and added DMCA_EMAIL to siteConfig.js.
- Flagged directly on the page itself: the actual DMCA designated-agent registration with the U.S. Copyright Office is a real filing that still needs to happen -- the policy page is built, but registering an agent is a business/legal action item, not something that can be done in code.

3. UNLISTED /privatedemo ROUTE FOR BACKER PITCHES
- Added a /privatedemo route in App.jsx that aliases the real HomePage -- the actual live product, not a mockup -- so Jeff has a stable link to share with potential backers before a public launch.
- Deliberately NOT added to INDEXABLE_STATIC_ROUTES, so it has no sitemap entry and no prerendered shell.
- Added a /privatedemo case to buildRouteMetadata() in seoMetadata.js with robots: "noindex,nofollow", and added "Disallow: /privatedemo" to robots.txt.
- Found and fixed a trap along the way: scripts/generate-sitemap.mjs regenerates public/robots.txt (and sitemap.xml) from a hardcoded array on every `npm run build`, silently overwriting any direct edit to the static robots.txt file. Fixed the actual source array in generate-sitemap.mjs instead, then rebuilt and confirmed /privatedemo appears in robots.txt's disallow list and nowhere in sitemap.xml.
- Verified live: the noindex,nofollow robots meta tag is actually present on the rendered /privatedemo page.

LATEST CONTENT-LICENSING VALIDATION COMPLETED
- npm run lint
- npm test (61 tests passing)
- npm run build (244 route shells prerendered, up one for the new /dmca page; confirmed /privatedemo has no sitemap entry)
- netlify dev + headless Playwright: opened a real article end-to-end and confirmed excerpt length/cutoff and the "Read the full story at [Source]" CTA; confirmed /privatedemo's noindex meta tag; screenshotted the rendered /dmca page

LATEST CONTENT-LICENSING OUTCOME
- The reader no longer reproduces full third-party articles anywhere in the product -- aggregation now matches the Google-News-style model Jeff described (credit, snippet, link out) rather than full mirroring.
- A real DMCA policy now exists and is discoverable from the footer; the one remaining step is registering an actual designated agent with the Copyright Office, which is Jeff's/legal's action item.
- Jeff has a private, non-indexed, non-sitemapped link to demo the real product to backers ahead of a public launch.
- Next planned work: extend the existing Story Dossier (cross-outlet coverage retrieval) into an original per-story synthesis feature ("The Latest's Take") that cites multiple outlets' framing of the same story -- a stronger transformative-use position than rewriting any single article, and a natural home for the value-add layer the trust/perspective differentiation is already built around.

"THE LATEST'S TAKE" -- ORIGINAL CROSS-OUTLET SYNTHESIS FEATURE (08/19/2026)
Followed through on the user's spitballed idea from the content-licensing discussion: instead of an AI rewrite of a single article (still legally risky if it tracks the original too closely), extend the existing Story Dossier's cross-outlet coverage retrieval into a genuine multi-source synthesis -- comparing how different outlets frame the same story, which is a materially stronger transformative-use position than paraphrasing any one source.

1. BACKEND: netlify/functions/storyTake.js (new)
- Reuses storyDossier.js's existing cross-outlet retrieval directly (calls storyDossier.handler(...) internally, the same way storyDossier itself calls rss-aggregator/clusters/fetchSocialFeeds) rather than re-implementing feed/cluster fetching -- no duplicated retrieval logic.
- Hard legal-safety guardrail: requires at least 2 distinct outlet names in the combined coverage/cluster data before it will even attempt generation. Below that threshold it returns `{ take: null, reason: "insufficient-cross-outlet-coverage" }` and the client simply shows nothing -- there is no path where this feature synthesizes from a single source.
- The prompt sent to Claude explicitly frames the task as multi-source comparison, not rewriting: "This must be your own original analysis. Do not closely paraphrase any single source's sentences, structure, or distinctive phrasing -- synthesize and compare across the outlets below instead," and requires the model to name 2-3 of the specific outlets whose coverage informed the take.
- Follows the exact conventions already established in generatedContent.js/sharedSummary.js: same getConfigValue/.env-fallback pattern for ANTHROPIC_API_KEY, same extractAnthropicTextBlocks parsing, same Netlify Blobs caching approach (STORE_NAMES.storyTakes, 45-minute freshness TTL) keyed by the dossier's own normalized query+category so repeat views of the same underlying story reuse one cached synthesis instead of re-billing Claude per click, and the same enforceRateLimit gating (20 requests/min/IP).
- If ANTHROPIC_API_KEY isn't configured, or Claude's response doesn't parse, it degrades to `{ take: null }` rather than erroring -- the rest of the page is completely unaffected either way.

2. FRONTEND: src/pages/ArticleReader.jsx / src/services/storyTakeService.js (new)
- New "The Latest's Take" card renders directly after the excerpt + "Read the full story" CTA and before the Story Dossier's related-coverage grid -- positioned as the value-add layer between "here's a snippet of the story" and "here's where else to read about it."
- Visually distinct (bordered, tinted card) from the plain dossier grid below it, with an explicit "Based on coverage from [outlets]" attribution line under the synthesis text.
- Fetches in parallel with the dossier (not sequentially after it) via its own effect keyed on the article; gated so it never runs for our own AI-generated articles (they don't need a synthesis of themselves).
- Fixed a real bug found by the new eslint-plugin-react-hooks `set-state-in-effect` rule during this work: an early-return branch was calling setState synchronously as a direct statement in the effect body. Restructured to wrap the fetch in a nested async function (matching the exact shape the existing, lint-clean Dossier-loading effect already uses) and to gate stale-state rendering by comparing the request's article identity (articleKey) against the current one, instead of resetting state synchronously on every unrelated re-render.

3. VERIFICATION
- Directly verified the full Claude generation path end-to-end (bypassing the live feeds' search-matching quality, which proved conservative in this dev environment) by feeding storyTake.js realistic 3-outlet coverage data via the same monkey-patch-before-require technique proven in the earlier rate-limit/auth test work. Result: a genuinely comparative synthesis that correctly identified each outlet's distinct framing (NYT's "symbolic threshold," NPR's "concrete economic consequences," The Guardian's "political gridlock" angle) and cited all three by name -- not a reworded copy of any one of them.
- Verified the real guardrail path live against an actual clicked article: the live dossier only found one confident outlet match for that story, and the feature correctly declined to generate anything rather than force a thin, single-source "synthesis."
- Verified the actual React rendering path (not just the API) via Playwright network-interception with realistic mocked data: confirmed the card renders correctly, found and fixed a real dark-mode contrast bug (`.ar-take-body` was using an undefined `--text-color` custom property instead of the same hardcoded light-on-dark value the rest of the article body already uses), re-verified with a clean, legible screenshot after the fix.
- Cleaned up a fabricated test cache entry from the live story-takes Blobs store afterward (kept the one real entry produced by an actual live click-through, since that one accurately reflects real system behavior).
- npm run lint / npm test (61 passing) / npm run build all clean after every change in this pass.

LATEST "THE LATEST'S TAKE" OUTCOME
- The trust/perspective differentiation now has a genuine value-add content layer to back it up: original, clearly-labeled, multi-source comparison -- not just links to other people's coverage.
- The legal posture here is deliberately more conservative than a single-article rewrite would be: no synthesis is ever generated from fewer than two distinct outlets, and the prompt itself forbids paraphrasing any one source.
- Runs on the same ANTHROPIC_API_KEY already configured and verified live in Netlify -- no new secret or third-party account needed to turn this on.

TRUTH-SCORE RETIREMENT, "THE COMPASS" REBUILD, AND SOURCE-REGISTRY EXPANSION (08/26/2026)
A full-day session driven by a mix of Jeff Hall feedback and direct user direction, covering: retiring the "truth score" framing, fixing a real AI-summary truncation bug, rebranding "Perspective" to "The Compass" in user-facing copy, expanding the curated source registry from 84 to 95 outlets, reworking mobile/header navigation, and fixing two real Compass data-accuracy bugs. Times below are approximate (drawn from actual tool/test-run timestamps during the session), all on 2026-08-26.

1. TRUTH SCORE FEATURE RETIRED (~1:15-1:40 PM)
- Jeff Hall flagged that "truth score" framing is legally/reputationally risky and too subjective to defend ("things that seem true today can turn out to be false later").
- Added a `TRUTH_SCORE_ENABLED = false` flag in src/utils/sourceProfiles.js. Scoring logic (factuality label, ownership, perspective inputs) was deliberately left intact -- only display was gated -- so it can be re-enabled with a one-line change later.
- Gated all ~14 render sites behind the flag: ArticleReader.jsx, SourceProfilePage.jsx (found and fixed a *second*, previously unguarded "Truth score: X/100" line in the Trust Snapshot section that the first pass had missed), NewsCard.jsx, the four All*Page.jsx list pages, and TopStories.jsx.
- Updated the one test asserting the badge rendered (NewsCard.test.jsx) to assert it's absent instead; updated README.md and WEBSITE_FEATURES_LIST.txt to document the flag and how to reverse it.

2. AI DAILY BRIEFING TRUNCATION BUG FIX (~1:40-2:00 PM)
- Jeff reported the homepage AI briefing rendering broken text: "Meta agreed to pay up to $17. 1 billion" (stray space) cutting off mid-sentence with no ellipsis.
- Root cause: the sentence-splitting regex in AISummary.jsx's buildCompactSummaryText treated any bare `.`/`!`/`?` as a sentence end, so it split "$17.1 billion" into "$17." + "1 billion" and, separately, split off part of "U.S." as its own fragment -- then silently dropped whatever fell past the 2-sentence cap. Reproduced the exact reported artifact with the old regex to confirm the diagnosis before fixing it.
- Fixed the regex to require whitespace + a capital letter/digit after the punctuation before treating it as a real sentence boundary; raised the display cap from 2 sentences/460 characters to 3/600 so there's room for the extra context.
- The backend (netlify/functions/sharedSummary.js) had the same class of bug independently, in its own character-count truncation with no word-boundary safety -- fixed truncateText/capSummaryText to back up to the last space instead of cutting mid-word, and updated the Claude prompt to explicitly require stating the reason behind settlements/lawsuits/resignations (not just the outcome) and to never shorten a figure. Raised the backend budget from 700 to 850 characters / 2-4 to 3-5 sentences to match.

3. "PERSPECTIVE" RENAMED TO "THE COMPASS" IN USER-FACING COPY (~2:00-2:45 PM)
- Verified (rather than assumed) that both "side by side" and "the compass" features actually exist and function, using a live headless-browser pass -- then, per direction, renamed the public-facing "Perspective" label to "Compass" everywhere a reader sees it, while deliberately leaving internal code untouched (perspectiveKey/perspectiveLabel data fields, CSS class names, the perspective.js function name) since renaming those was pure risk with no user-facing benefit.
- Updated ~8 literal strings: the "See Multiple Perspectives" toggle and "Perspective estimates" badge in TopStories.jsx, the side-by-side title in HomePage.jsx, "Perspective methodology" in ArticleReader.jsx, "Perspective label" in SourceProfilePage.jsx, the methodology copy in sourceProfiles.js, and the "Perspective not classified..." string duplicated across NewsCard.jsx and the four All*Page.jsx files.
- Deliberately left "Search For Perspective" in SearchResults.jsx untouched -- confirmed it's an unrelated search-tip title about opinion pieces, not the compass feature, so renaming it would have mislabeled a different feature.
- Verified live via headless browser before and after (toggle text, badge text, zero console errors).

4. SOURCE REGISTRY EXPANSION -- 84 TO 95 CURATED SOURCES (~2:45-4:00 PM)
- Added MS NOW after discovering mid-task that MSNBC officially rebranded in November 2025 and spun off from NBCUniversal into a new public company, Versant Media Group, in January 2026 -- the registry now reflects the current name/ownership, with "msnbc" kept as a matching alias.
- Researched and added, each with a verified live RSS feed (independently re-curl'd, not just trusted from research-agent output), real ownership facts, and multi-tracker (AllSides / Ad Fontes Media / Media Bias/Fact Check) bias-and-reliability calibration against existing registry precedent:
  - Newsmax -- Right-Center, but "Low factuality" (worse than Fox), reflecting a $67M Dominion + ~$40M Smartmatic defamation-settlement record and its own feed's flaky reachability (kept, but marked low-priority).
  - PBS NewsHour -- Left-Center, after catching and correcting an earlier mistaken claim (made mid-research) that NPR was rated "Center" -- NPR is actually Left-Center, so PBS was matched to that real precedent instead.
  - Axios, HuffPost, Semafor -- Center / Left-Center per tracker consensus, following the same "plain established label over inventing a new nuanced one" rule applied consistently across this batch.
  - The Daily Wire -- pure "Right" (all three trackers agree, no moderating "-Center" rating), matching National Review's tier rather than Fox's.
  - Mother Jones -- the registry's first pure "Left" entry, deliberately mirroring National Review on the right after confirming 2 of 3 trackers support it.
  - Time, Newsweek (flagged explicitly as a genuinely disputed/mixed case -- trackers disagree and it has a documented correction history, not a clean "mainstream = safe" pick), Yahoo News, and Chicago Tribune (added for classification purposes only -- every RSS feed path tested returns HTTP 403, so no live feed is wired up for it yet).
- Registry grew from 84 to 95 sources; all new RSS feed URLs were independently verified live via direct curl before being added to netlify/functions/rss-aggregator-impl.cjs, separate from whatever a research pass had already claimed.

5. MOBILE FOOTER HIDDEN; LINKS MOVED TO THE SECTIONS PAGE (~4:00-4:20 PM)
- Hid the desktop Footer below 768px (Footer.css) -- the same breakpoint the bottom-dock mobile nav already uses -- and added an equivalent "About & site info" links block to NewsPage.jsx (the page the bottom dock's existing "Sections" tab already points to), visible only on mobile so desktop is unaffected.

6. DEDICATED /compass PAGE AND PERMANENT HEADER NAV ENTRY (~4:20-5:00 PM)
- Investigated real-world Compass discoverability rather than assuming: found the toggle only ever appears on the homepage and (once fixed in item 9 below) the Politics category page, contingent on live cluster data existing that day, with zero mentions anywhere in the header nav, About page, or Editorial Standards page.
- Built a new standalone src/pages/CompassPage.jsx at /compass that always opens directly into the comparison view, fed by the same general-news cluster query the homepage already uses -- chosen over opening the toggle on every category page specifically to avoid adding new RSS load to pages that don't currently fetch cluster data.
- Added a persistent "Compass" icon+label pill to the header's utilities row (next to search/theme/profile) instead of as an 8th item in the category nav strip, after measuring live that the nav strip already overflows into horizontal scroll below ~1180px width and confirming the search bar's own width is completely unaffected either way (it has protected, non-shared width).
- Restyled the on-page "See the Compass" / "Back to Top Stories" toggle button in TopStories.jsx to match the new header pill's shape, padding, and icon treatment.

7. HEADER ICON AND SEARCH-BAR OVERHAUL (~5:00-5:45 PM)
- Swapped the header's hand-drawn profile SVG for FontAwesome's solid "user" icon at the exact same 28px-in-34px footprint; swapped the Compass icon to the regular/outline style (adding the previously-uninstalled @fortawesome/free-regular-svg-icons package); confirmed the moon and magnifying-glass icons already matched the requested reference styles with no change needed.
- Made all four header utility icons (Compass, search, theme toggle, profile) render at an explicit, identical 17px font-size, replacing a mix of hardcoded pixel boxes and inherited browser defaults that had made them visibly different sizes.
- Rebuilt the header search bar to collapse to a 40x40 icon button by default on desktop/tablet, expanding on click (auto-focused) and collapsing again on outside-click, Escape, or after a search submits. This is what actually solved the nav-overflow problem in practice: verified live that all 7 category nav items now fit with zero clipping at 1300px width, where "Lifestyle" used to get cut off before. Left mobile's always-expanded search bar completely untouched, since there's no nav-space pressure to solve there.

8. LAYOUT/SPACING FIXES ON THE COMPASS AND CATEGORY-PAGE UI (~5:45-6:05 PM)
- Added padding to the Compass page's title row and Filter row (TopStories.css).
- Found and fixed the real cause of PageBackBar's grey background leaving white gaps on both edges on every category/topic/search page: the shared `.main-content` wrapper's own horizontal padding was being applied to the bar itself. Fixed with a matching negative margin so the background bleeds full-width while the bar's inner content (Back button, breadcrumb, count) stays inset exactly where it was.

9. COMPASS DATA-ACCURACY FIXES (~6:05-6:20 PM)
- Verified with live API calls (not assumptions) whether Compass actually works on the News and Politics category pages, and whether there's enough real source coverage to produce genuine comparisons.
- Found and fixed a real bug: the Politics category page's cluster fetch (CategoryPage.jsx) was searching the literal text "Politics" against article titles/descriptions -- confirmed via direct API call this returns 0 stories, since political articles rarely contain the word "politics" itself. Fixed to filter by the actual `politics` RSS category instead, confirmed live this now returns 129 stories and clusters with up to 7 sources. The News/Top Stories category still intentionally has no Compass toggle at all (an earlier, deliberate scope decision to avoid extra RSS load) -- confirmed its underlying general-news query works just as well if that decision is ever revisited.
- Found a second, more significant bug: netlify/functions/perspective.js (which labels each Compass source Left/Center/Right) ran its own tiny ~24-outlet hardcoded map plus an AI classifier, completely disconnected from the 95-source sourceProfiles.js registry built earlier this session -- and trusted an uncertain AI guess over a confident hardcoded match whenever both were available, so well-known, already-correctly-classified outlets (New York Times, The Hill) were showing "Unclassified" in the live Compass view.
- Fixed by having perspective.js dynamically import sourceProfiles.js (the same proven pattern already used in rss-aggregator-impl.cjs) and check the full registry first; AI classification and the small legacy map now only run for outlets genuinely absent from the registry. Verified directly: New York Times, Newsmax, The Hill, Mother Jones, and MS NOW (matched via its old "MSNBC" alias) all now resolve instantly and correctly at zero AI cost; a made-up source name still correctly falls through to AI. Re-verified against the live Politics page's real cluster data afterward.
- Flagged, but did not fix in this pass, a smaller separate issue found while verifying: a handful of politics-category feed items carry auto-derived, unspaced source names ("Thehill", "Motherjones") that don't match the registry's spaced aliases -- noted as a follow-up, not blocking.

LATEST COMPASS/SOURCE-REGISTRY SESSION VALIDATION COMPLETED
- npm run lint (clean after every change in this session)
- npm test / npx vitest run -- 61 tests passing throughout, including one test updated for the new two-step collapsible-search interaction and two updated for the "Compass" copy rename
- Live netlify dev + headless Playwright verification after nearly every change: truth-score badges absent, AI summary text no longer garbled, Compass toggle/labels/icons rendering correctly at desktop/tablet/mobile widths, header nav fitting without clipping at 1300px, PageBackBar spanning full width, Politics-page clusters populated with correct Left/Center/Right labels
- Independent direct-curl verification of every new RSS feed URL, separate from research-agent claims
- Direct Node-level testing of netlify/functions/perspective.js's fixed classification logic against known and unknown source names

LATEST COMPASS/SOURCE-REGISTRY SESSION OUTCOME
- "Truth score" is fully retired from the UI (logic preserved for a future decision) without losing any of the underlying trust-scoring work.
- The AI daily briefing no longer garbles numbers or cuts off mid-sentence, and its prompt now explicitly asks for the "why" behind major stories.
- "The Compass" is now a real, permanently-discoverable feature (persistent header nav entry, dedicated /compass page) instead of something that only sometimes appeared on the homepage -- and its Left/Center/Right labels are now backed by the same 95-source curated registry used everywhere else on the site, instead of a disconnected, stale, AI-second-guessed 24-outlet list.
- The Politics category page's comparison view went from completely broken (0 clusters) to fully working with correctly-labeled sources, found and fixed via direct verification rather than assumption.
- One known, smaller follow-up remains open: a handful of auto-derived source names in the politics-category feed pipeline don't match registry aliases due to missing spaces (e.g. "Thehill").
